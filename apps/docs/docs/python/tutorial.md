# Tutorial

::: warning Coming soon
The step-by-step Python tutorial is still being written.
:::

In the meantime:

- [First Steps](/python/get-started) walks through installing the package and
  building your first chart.
- The [API Reference](/python/api/core/chart) documents every operator and mark.
- The [JavaScript tutorial](/js/tutorial) covers the same concepts — the API is
  the same, only the syntax differs (keyword arguments instead of option
  objects). Use the toggle at the top to switch.
